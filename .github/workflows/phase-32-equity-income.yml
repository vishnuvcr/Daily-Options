name: Phase 32 - Nested walk-forward

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase-32-equity-income-wfa-v1
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Phase definition
        run: |
          echo "Phase 32: Nested walk-forward"
          echo "Manual execution branch: phase-32-equity-income-wfa-v1"
          echo "Weekly target: INR 5,000 net per traded week at fixed reference sizing"
          echo "Scope: Perform train/validation/test selection without test leakage."
